# 資料庫配置
# 本地開發：使用 localhost
# Docker 部署：使用服務名稱（例如：mysql）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ChatPTT
DB_USERNAME=User
DB_PASSWORD=your_password_here

# 服務端口
# Railway 會自動設置 PORT 環境變數，這裡是本地開發的預設值
PORT=8080

# Railway 部署說明：
# 在 Railway 上設置環境變數時，使用以下格式：
# DB_HOST=${{MySQL.MYSQLHOST}}
# DB_PORT=${{MySQL.MYSQLPORT}}
# DB_NAME=${{MySQL.MYSQLDATABASE}}
# DB_USERNAME=${{MySQL.MYSQLUSER}}
# DB_PASSWORD=${{MySQL.MYSQLPASSWORD}}
# PORT=${{PORT}}

